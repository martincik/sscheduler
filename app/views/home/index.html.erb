<h2>List of products on Your shopify</h2>

<% form_tag url_for(:action => 'set_schedule') do -%>

<table cellpadding="1" cellspacing="1" id="product-table">
  <thead>
    <tr>
      <th>&nbsp;</th>
      <th>Name</th>
      <th>Price</th>
      <th>Type</th>
      <th>Visible from</th>
      <th>Visible to</th>
      <th>Published</th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
    <tr>
      <td>
        <input type='checkbox' id="products_<%= product.id %>" name="products[<%= product.id %>]" value="1"
          <%= "checked=true" if @products_ids.include?(product.id.to_s) %>
          <%= "class=#{@checked_products}" %>
        >
      </td>
      <td><%= link_to product.title, "http://#{current_shop.url}/admin/products/#{product.id}" %></td>
      <td><%= product.price_range %> <%= current_shop.shop.currency %></td>
      <td><%= product.product_type %></td>
      <td><%= @scheduled_products[product.id.to_s].nil? ? '---' : hdatetime(@scheduled_products[product.id.to_s].from_time) %></td>
      <td><%= @scheduled_products[product.id.to_s].nil? ? '---' : hdatetime(@scheduled_products[product.id.to_s].to_time) %></td>
      <td><%= is_ok_tag product.published_at %></td>
    </tr>
    <% end -%>
  </tbody>
</table>

<div id="schedule_form">
  <label>Scheduled from</label>
  <div>
    <%= text_field_tag 'from_time', @from_time, {:class => @checked_dates } %>
    <%= text_field_tag 'from_date', @from_date, {:class => @checked_dates } %>
  </div>
  <label>Scheduled to</label>
  <div>
    <%= text_field_tag 'to_time', @to_time, {:class => @checked_dates } %>
    <%= text_field_tag 'to_date', @to_date, {:class => @checked_dates } %>
  </div>
  <br />
  <%= submit_tag 'unschedule', {:id => 'unschedule'} %>
  <%= submit_tag 'schedule', {:id => 'schedule'} %>
  <%= javascript_tag '$("#from_time").ptTimeSelect(); $("#to_time").ptTimeSelect();' %>
  <%= javascript_tag '$("#from_date").datepicker({ dateFormat: "dd/mm/yy" }); $("#to_date").datepicker({ dateFormat: "dd/mm/yy" });' %>
</div>
<br style="clear: both;" /><br />

<% end -%>

